import styles from './IconButton.module.css'

interface Props {

}

const IconButton = ({
    
}: Props) => {
    return (
        <button className={styles.iconButton}>
            
        </button>
    )
}

export default IconButton